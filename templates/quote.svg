<g id="quote" transform="matrix{{ commons.matrix_quote }}">
    {% for line in commons.getQuote() %}
        <text class="text" transform="translate(0 {{ loop.index0*20 }})">{{line}}</text>
    {% endfor %}
</g>
